begin
SET active TO true;

do;
    if P then;
        SET active TO false;
    else;
        if not Q then;
            if R then;
                SET active TO false;
            fi
        fi
    fi
while active then;
od

end
